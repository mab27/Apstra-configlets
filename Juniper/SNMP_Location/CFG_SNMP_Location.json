{
    "display_name": "CFG_SNMP_Location",
    "ref_archs": [
        "two_stage_l3clos"
    ],
    "generators": [
        {
            "config_style": "junos",
            "section": "system",
            "template_text": "{% if SNMP_Location[hostname] is defined %} \nsnmp {\n    location \"{{SNMP_Location[hostname]}}\"\n}\n{% endif %}",
        }
    ]
}
