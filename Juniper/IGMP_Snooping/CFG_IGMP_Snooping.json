{
    "display_name": "CFG_IGMP_Snooping",
    "ref_archs": [
        "two_stage_l3clos"
    ],
    "generators": [
        {
            "config_style": "junos",
            "section": "system",
            "template_text": "{% for vlan_id, vn_data in vlan.items() %}\n    {% if 'mcast' in vn_data['name'] %}\nprotocols {\n    igmp-snooping {\n    vlan vn{{ vlan_id }} \n    }\n}\n    {% endif %}\n{% endfor %}"
        }
    ]
}